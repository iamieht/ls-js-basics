// What happens if we take the array ['fish', 'and', 'chips'] and try to access the element at index position 10?

console.log(["fish", "and", "chips"][3]);

// We won't get any error message, but the expression will return undefined.
